export type TService = {
  _id: string;
  name: string;
  image: string;
  description: string;
  features: string[];
};

export type TServiceInput = {
  name: string;
  image: string;
  description: string;
  features: string[];
};
